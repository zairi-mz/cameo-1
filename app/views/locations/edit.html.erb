<h1>Editing location</h1>

<% form_for(@location) do |f| %>
  <%= f.error_messages %>
<!-->
  <p>
    <% f.label :code %><br />
    <% f.text_field :code %>
  </p> -->
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <% f.label :orgchart_id %>Department<br />
    <%= f.collection_select :orgchart_id, Orgchart.find(:all), :id, :name, :include_blank => true %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @location %> |
<%= link_to 'Back', locations_path %>