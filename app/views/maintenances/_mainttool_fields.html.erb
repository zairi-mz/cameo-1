<div class="fields">
	<p>|	Tool  :  <%= f.collection_select :tool_id, Tool.find(:all, :order => :name), :id, :name, :include_blank => true %>
			<%= f.hidden_field :_destroy %>  
		| 	<%= link_to_function image_tag("delete.png", :border => 0, :title => 'Remove'), "remove_fields(this)"%></p>
</div>