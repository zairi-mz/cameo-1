<div class='webform'>
<h2>Edit <%= t ('documents.doc') %></h2>
<% form_for(@document) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => 'form', :locals => {:f => f}   %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>
</div>

<%= link_to 'Show', @document %> |
<%= link_to 'Back', documents_path %>